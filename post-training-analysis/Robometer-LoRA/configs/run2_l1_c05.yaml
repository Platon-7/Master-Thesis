# Bake-off Run 2 — L1 (CORN) mild asymmetry.
# Apply on top of train_lora_base.yaml + loss1_corn.yaml.
#   * c=0.5 → α_k = (1, 1.5, 2.0, 2.5) for k=2..5 (overrides loss1_corn.yaml's 1.5 default).
#   * lr=4e-5.
#   * ICL task-instruction dropout: OFF.

loss.corn_asymmetric_c: 0.5
training.learning_rate: 4.0e-5
training.max_steps: 7500
training.exp_name: bakeoff_run2_l1_c05
