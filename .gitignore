video-generation/
# Python
__pycache__/
*.pyc
*.pyo
*.egg-info/

# IDE
.idea/
.vscode/
*.swp
*.py~
.DS_Store

# Logs and outputs
*.out
real-world-data-generation/logs/
dsrl/wandb/

# Sample/test data (binary)
real-world-data-generation/sample_videos/
real-world-data-generation/test_relabel/

# Droid failures data (large, stored locally)
real-world-data-generation/droid_failures/keyframes/
real-world-data-generation/droid_failures/videos/
real-world-data-generation/droid_failures/*.jsonl
real-world-data-generation/droid_failures/*.txt

# Temporary/review files
real-world-data-generation/droid_failures/sample_video_map.json
real-world-data-generation/droid_failures/sample_videos/
real-world-data-generation/droid_failures/task_review*.csv
real-world-data-generation/droid_failures/labeling_status.md

# Large generated visualizations and data files
simulated-data-generation/MetaWorld/logs/*.html
simulated-data-generation/MetaWorld/logs/*.out
*.jsonl

# Ctrl-World (embedded git repo — add as submodule if needed)
Ctrl-World/

# PlayWorld generated videos (large binaries)
PlayWorld/videos/
PlayWorld/verification_traj1/*.mp4

# Model/data artifacts (stored on scratch)
*.safetensors
*.bin
*.gguf

# Node
node_modules/
package-lock.json
package.json

# External dependency vendored into Failsafe (360 MB ManiSkill install)
simulated-data-generation/Failsafe/failsafe_repo/

# Wandb run artifacts (cloud-synced; only the run dirs are local noise)
**/wandb/

# SLURM logs (per-job .out/.err, regenerated each run; existing pattern *.out covers
# .out files everywhere but not the matching .err logs — be explicit)
post-training-analysis/Robometer-LoRA/logs/
Robometer/logs/
simulated-data-generation/Failsafe/logs/
real-world-data-generation/Robometer/logs/
real-world-data-generation/Robometer/rbm-1m-ood-pipeline/logs/
real-world-data-generation/Droid-Failures/logs/

# Checkpoints (often huge, scratch-storage anyway)
**/checkpoint-*/
**/ckpt-*/

# Per-rank memory logs that the Robometer trainer writes
**/memory_log_rank*.txt

# Vendored MetaWorld package — install via pip if needed, don't track 294 MB
simulated-data-generation/MetaWorld/metaworld_repo/

# Local Claude Code guidance — kept on disk but not pushed
CLAUDE.md

# Claude Code session state (local-only)
.claude/

# Generated visualizations (>100MB, regenerable)
simulated-data-generation/Failsafe/failsafe_multicam.html

# Qwen35-FT noise (logs / training outputs / wandb already covered by **/wandb/)
Qwen35-FT/logs/
Qwen35-FT/outputs/

# Generic outputs/ dirs (training checkpoints, eval dumps, etc.)
**/outputs/

# Pre-revert backup (1.1 GB) — kept locally, never push
Robometer.bak_pre_revert/

# Unsloth bytecode cache — auto-regenerated on first import
**/unsloth_compiled_cache/

# Top-level SLURM logs dir (per-job artifacts, regenerated each run)
/logs/
Robometer-FT/logs/

# Diagnostic image dumps from Robometer/scripts/sample_*.py — regenerable, large
droid_samples/
metaworld_samples/

# Generated analysis artifacts (plots + data dumps) — keep scripts + docs only
loss-debug/*.png
loss-debug/*.pdf
loss-debug/*.csv
loss-debug/sp_dist/
loss-debug/score4_diag/
vlm_ibrl/results/
Qwen35-FT/scripts/_out/

# --- appended: exclude large data / junk / third-party nested repo ---
# Third-party nested repo (has its own .git; not part of this codebase)
Robo-Dopamine/
# Large binary rollout/result data
*.npz
post-training-analysis/reward-model-study/results/robomimic/
# MuJoCo runtime logs
MUJOCO_LOG.TXT
**/MUJOCO_LOG.TXT
# C/C++ build artifacts
**/common_utils/build/
*.err

# RoboRewardBench run outputs and runtime caches
RoboReward/roborewardbench/logs/
**/unsloth_compiled_cache/
