video-generation/
# Python
__pycache__/
*.pyc
*.pyo
*.egg-info/

# IDE
.idea/
.vscode/
*.swp
*.py~
.DS_Store

# Logs and outputs
*.out
Real-World-Failures/logs/
dsrl/wandb/

# Sample/test data (binary)
Real-World-Failures/sample_videos/
Real-World-Failures/test_relabel/

# Droid failures data (large, stored locally)
Real-World-Failures/droid_failures/keyframes/
Real-World-Failures/droid_failures/videos/
Real-World-Failures/droid_failures/*.jsonl
Real-World-Failures/droid_failures/*.txt

# Temporary/review files
Real-World-Failures/droid_failures/sample_video_map.json
Real-World-Failures/droid_failures/sample_videos/
Real-World-Failures/droid_failures/task_review*.csv
Real-World-Failures/droid_failures/labeling_status.md

# Large generated visualizations and data files
MetaWorld/logs/*.html
MetaWorld/logs/*.out
*.jsonl

# Ctrl-World (embedded git repo — add as submodule if needed)
Ctrl-World/

# PlayWorld generated videos (large binaries)
PlayWorld/videos/
PlayWorld/verification_traj1/*.mp4

# Model/data artifacts (stored on scratch)
*.safetensors
*.bin
*.gguf

# Node
node_modules/
package-lock.json
package.json

# External dependency vendored into Failsafe (360 MB ManiSkill install)
Failsafe/failsafe_repo/

# Wandb run artifacts (cloud-synced; only the run dirs are local noise)
**/wandb/

# SLURM logs (per-job .out/.err, regenerated each run; existing pattern *.out covers
# .out files everywhere but not the matching .err logs — be explicit)
Robometer-LoRA/logs/
Robometer/logs/
Failsafe/logs/
Real-World-Failures/Robometer/logs/
Real-World-Failures/Droid-Failures/logs/

# Checkpoints (often huge, scratch-storage anyway)
**/checkpoint-*/
**/ckpt-*/

# Per-rank memory logs that the Robometer trainer writes
**/memory_log_rank*.txt

# Vendored MetaWorld package — install via pip if needed, don't track 294 MB
MetaWorld/metaworld_repo/

# Local Claude Code guidance — kept on disk but not pushed
CLAUDE.md
