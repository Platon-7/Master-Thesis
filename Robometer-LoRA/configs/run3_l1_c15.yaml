# Bake-off Run 3 — L1 (CORN) strong asymmetry.
# Apply on top of train_lora_base.yaml + loss1_corn.yaml.
#   * c=1.5 → α_k = (1, 2.5, 4, 5.5) for k=2..5 (matches loss1_corn.yaml default; explicit for traceability).
#   * lr=4e-5.
#   * ICL task-instruction dropout: OFF.

loss.corn_asymmetric_c: 1.5
training.learning_rate: 4.0e-5
training.max_steps: 4000
training.exp_name: bakeoff_run3_l1_c15
